package language

/*
import "golang.org/x/text/language"

var supportLanguages map[string]language.Tag

func init() {
	supportLanguages = make(map[string]language.Tag)
	supportLanguages["en"] = language.English
}

func IsSupportLanguage(lang string) bool {
	if _, ok := supportLanguages[lang]; ok {
		return true
	} else {
		return false
	}
}

func StringToLanguage(str string) language.Tag {
	if lang, ok := supportLanguages[str]; ok {
		return lang
	} else {
		return language.English
	}
}
*/
